import setuptools

setuptools.setup(
        name='autograde',
        author='Team AG',
        packages=['autograde'],
        install_requires=[
            'Pillow',
            'matplotlib',
            'sklearn'])
